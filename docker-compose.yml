services:

  portfolio:
    build:
      context: ./portfolio

    container_name: lifted-portfolio

    ports:
      - "8082:80"

    restart: unless-stopped


  java-app:
    build:
      context: ./java-app

    container_name: lifted-java-app

    ports:
      - "8081:8081"

    restart: unless-stopped